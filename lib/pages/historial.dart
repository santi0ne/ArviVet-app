import 'package:flutter/material.dart';

class VetHistorialPage extends StatelessWidget {
  const VetHistorialPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Vista de Historial',
        style: TextStyle(
          fontSize: 22,
          fontWeight: FontWeight.bold,
          color: Colors.black87,
        ),
      ),
    );
  }
}